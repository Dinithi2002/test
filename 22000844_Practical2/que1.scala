object que1{
    def main(args:Array[String]){
        var i, j, k: Int = 2
        var m, n: Int = 5
        var f: Float = 12.0f
        var g: Float = 4.0f
        var c: Char = 'X'

        var a = k + 12 * m
        var b = m / j
        var c2 = n % j
        var d = m / j * j
        var e = f + 10 * 5 + g
        i += 1 
        var p = i * n

        println("k + 12 * m = "+a)
        println(" m / j     = "+b)
        println("n % j      = "+c2)
        println("m / j * j  = "+d)
        println("f+10*5 + g = "+e)
        println("++i * n    = "+p)

    }

}